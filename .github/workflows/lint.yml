name: Lint

on: [push]

jobs:
  vue-cli:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Install
      run: |
        npm ci
    - name: Lint
      run: |
        npm run lint